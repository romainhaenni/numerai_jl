# Numerai Tournament Configuration Example
#
# Copy this file to config.toml and update with your settings

# API Credentials (can also be set via environment variables NUMERAI_PUBLIC_ID and NUMERAI_SECRET_KEY)
api_public_key = "I3MEFCUZJQ4BEU5TSO7MY7XDIU2UBV7E"
api_secret_key = "337BEF4U2RZO6VOUHJGSOHAIWB7XT2M56PNL66HMS6KVWPZRC4XPMVEIVDLVSBHE"

# Tournament Configuration
# 8 = Classic Tournament (default)
# 11 = Signals Tournament
tournament_id = 8

# Model names to manage (leave empty to auto-discover from your Numerai account)
models = ["my_model_1", "my_model_2"]

# Data storage directories
data_dir = "data"
model_dir = "models"

# Automation settings
auto_submit = true
stake_amount = 10.0

# Feature set configuration
# Options: "small" (~300 features), "medium" (~1000 features), "all" (~5000 features)
feature_set = "medium"
